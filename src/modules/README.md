# Modules

This directory contains feature-based modules for the application.
Each module should group related components, services, repositories, types, validators, hooks, and utils.

Example:

- blog/
  - components/
  - services/
  - repositories/
  - types/
  - validators/
  - hooks/
  - utils/
